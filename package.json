{
  "name": "rupeedesk",
  "version": "1.0.0",
  "private": true,
  "description": "Rupeedesk web app packaged for Capacitor Android build",
  "scripts": {
    "build": "echo 'no build step; web assets in www/'",
    "prepare": "npx cap sync android || true"
  },
  "dependencies": {
    "@capacitor/core": "^5.6.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.6.0"
  }
}